# Main docker-compose.yml
# This file references the docker-compose configuration in infra/docker/
# For the full configuration, see: infra/docker/docker-compose.yml
#
# To run the stack:
#   docker-compose -f infra/docker/docker-compose.yml up
#
# Or use the convenience script:
#   ./scripts/start.sh

version: "3.9"

# This file is kept for backward compatibility
# The main docker-compose configuration is in infra/docker/docker-compose.yml
# See that file for the complete service definitions
